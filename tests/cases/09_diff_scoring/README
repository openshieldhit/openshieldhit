Differential scoring test case.

Exercises the Diff1 / Diff1Type parser and runtime for EKIN, LET, and QEFF
differential axes.  Runs with the same geometry and beam as case 00_minimal.

The case is run with the default args (transport enabled, short run).
