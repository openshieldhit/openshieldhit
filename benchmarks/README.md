# Benchmarks

Benchmark and validation infrastructure, organised by concern:

| Directory | Purpose |
|-----------|---------|
| `performance/` | Timing harness: scenario matrix, callgrind / perf profiles, throughput regression |
| `validation/` | *(future)* Physics correctness vs reference data (depth-dose, cross sections) |
| `regression/` | *(future)* Scored-output stability across commits |
