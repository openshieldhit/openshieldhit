# --- AI agent / tool configuration (local, not tracked) ---
# Tool/runtime config areas — permissions, hooks, machine-specific settings —
# stay out of version control. Project *guidance* for agents is tracked instead:
# CLAUDE.md, AGENTS.md, and .github/copilot-instructions.md, all thin pointers to
# DEVELOPER.md (the authoritative ruleset). Keep the split: config here, policy in
# those tracked Markdown files.
.claude/
.claude*/
.codex/
.codex*/

_temp*/
NB*
*.bdo
*.log
build-*/
build_*/
perf.data*
*.pyc
*.pdf

# Created by https://www.toptal.com/developers/gitignore/api/visualstudiocode,cmake,linux
# Edit at https://www.toptal.com/developers/gitignore?templates=visualstudiocode,cmake,linux

### CMake ###
CMakeLists.txt.user
CMakeCache.txt
CMakeFiles
CMakeScripts
Testing
Makefile
cmake_install.cmake
install_manifest.txt
compile_commands.json
CTestTestfile.cmake
_deps

### CMake Patch ###
CMakeUserPresets.json

# External projects
*-prefix/

### Linux ###
*~

# temporary files which can be created if a process still has a handle open of a deleted file
.fuse_hidden*

# KDE directory preferences
.directory

# Linux trash folder which might appear on any partition or disk
.Trash-*

# .nfs files are created when an open file is removed but is still being accessed
.nfs*

### VisualStudioCode ###
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets

# Local History for Visual Studio Code
.history/

# Built Visual Studio Code Extensions
*.vsix

### VisualStudioCode Patch ###
# Ignore all local history of files
.history
.ionide

# End of https://www.toptal.com/developers/gitignore/api/visualstudiocode,cmake,linux


# Build directory (CMake)
build/
# Generated documentation
docs/api/
site/

# DICOM test fixtures — fetched on demand from a GitHub release, never committed
# (see cmake/fetch_fixtures.cmake and tests/fixtures/dicom/README.md)
tests/fixtures/dicom/DCPT_headphantom/
