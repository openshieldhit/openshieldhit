# Build backend: scikit-build-core drives CMake directly, making it the natural
# choice for C/C++ projects with Python bindings. No bindings exist yet, but
# the infrastructure is in place for when they are added.
[build-system]
requires = ["scikit-build-core>=0.10", "setuptools-scm"]
build-backend = "scikit_build_core.build"

[project]
name = "openshieldhit"
description = "Lean Monte Carlo Particle Transport in C"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
dynamic = ["version"]

[project.optional-dependencies]
docs = [
    "mkdocs-material>=9.5,<10",
]

[tool.scikit-build.metadata.version]
provider = "scikit_build_core.metadata.setuptools_scm"

[tool.setuptools_scm]
fallback_version = "0.0.0"

[tool.scikit-build]
cmake.build-type = "Release"
cmake.args = ["-DOSH_BUILD_EXAMPLES=OFF"]  # examples are developer tools, not shipped in any release
wheel.packages = []          # no Python packages yet; remove when bindings are added

[tool.ruff]
line-length = 127

[tool.ruff.format]
preview = true
